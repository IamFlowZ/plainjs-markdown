{
  "name": "simple",
  "version": "1.0.0",
  "description": "Simple mardown editor with preview",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "updateSource": "git add .; echo Please enter a commit message:; read msg; git commit -m $msg; git push origin master;"
  },
  "author": "Dakota Lewallen",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.19.0"
  }
}
